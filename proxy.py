# Intemediário entre a chamada do obj(cliente) e a chamada do obj(servidor).
# Finge ser o objeto real. Costuma ser usado para controle de acesso, log, cache, lazy instantiation e lazy evaluation
# Se algo precisa ser disparado para vários serviços o proxy pode ser útil
# Pode escolher quando e como repassar chamadas pra o objeto real



